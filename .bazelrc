# We depend upon OpenJDK 21 and want to target that version for all builds
build --java_language_version=21
build --java_runtime_version=remotejdk_21

# We depend on Python 3 and want to target that version for all builds
build --host_force_python=PY3
build --python_version=PY3
